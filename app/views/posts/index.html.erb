<h1>Posts Index</h1>
<%= link_to "New tweet", new_post_path %>

<% @posts.each do |post| %>
	<div class="post pull-left">
		<h2><%= post.title %></h2>
		<small> <%= post.created_at.strftime("%d %b %Y") %></small>
		<p><%= post.text %></p>
	</div>
<% end %>